“请为我创建一个自定义技能 /worktree-flow。逻辑如下：

接受 prompt作为参数，prompt中描述了在worktree中的工作的要求。

根据 prompt 聚合新分支的名称（例如 feat-xyz）。

在主项目同级目录下创建 worktree：git worktree add ../wt-{branch_name} -b {branch_name}。

自动切换到该 worktree 目录。

进入计划模型，根据 prompt 和我讨论和确认计划，计划不但要保存在你自己的内部数据中，而且要在docs目录下形成md记录。
计划中要包含 TODOLIST, 这个形成单独的执行md文件，文件中使用 [ ] 作为未完成标记，完成的步骤使用标记 [x] 标识完成，这样可以避免开发进程异常中断如何接续的问题。

计划确认后，开始开发以及针对新新功能编写覆盖率不少于90%的测试程序。

开发阶段性完成后，要运行测试程序，发现问题，解决问题，迭代测试和修复直到所有测试通过。

全部完成测试后，要做 代码审查。并形成审查报告，写到docs目录中形成md文档。

请将此技能保存到你的全局配置或当前会话的自定义命令中，不要修改 CLAUDE.md。”

