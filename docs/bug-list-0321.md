## App 标题

固定为 dodo-box, 去掉后面的 - Zero Nostr Onboarding 字样。
不要说自己是 Nostr, 没有必要在 app 中说自己使用了什么技术。


## 登录

有时候登录行为发生的时候，显示找不到账户。再次点击登录，大部分时候会成功。
期待结果，当用户名密码输入正确的时候，能登录成功。


## 联系人列表

第一次添加别的人 identity，列表中能正确显示昵称为标题。
切换到其他界面，再回来，标题又成了 十六进制 串。
期待 列表项标题能保持 昵称显示，便于选择。

## 消息列表

消息累表中的的标题文字是 af51a1e2... 之类的诡异名称，不是昵称。
期待结果是 通讯录中的昵称

## 聊天窗口

### 输入
输入文字没有支持多行，当粘贴多行文本的时候，显示不恰当。
期待行为，支持多行文本；当输入多行文本的时候，输入框自动扩展多行行高，最多 4 行。

### 刷新

有消息丢失的情况。我目前不知道你刷新的逻辑是什么。为什么会丢失消息。
目前测试很容易复现，快速的输入回车，再输入回车，连续发消息，就会出现对方接到部分消息的情况。

期待，所有的消息都能被接收到。

